import React from 'react'
import { Icon } from '@ui-kitten/components'

const HomeIcon = (props) => <Icon {...props} name='log-in' />
const LoginIcon = (props) => <Icon {...props} name='log-in' />
const BackIcon = (props) => <Icon {...props} name='arrow-ios-back' />

export default {
  BackIcon,
  HomeIcon,
  LoginIcon,
}
